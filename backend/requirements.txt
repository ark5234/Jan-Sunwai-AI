# ── Runtime (live API) ─────────────────────────────────────────────────────
fastapi==0.104.1
uvicorn==0.24.0
gunicorn==25.3.0
python-multipart==0.0.6
Pillow==11.0.0          # EXIF geotagging + image validation
geopy==2.4.1
pymongo==4.16.0
ollama==0.6.1
requests==2.32.3
motor==3.7.1
email-validator==2.3.0
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
pandas==3.0.1          # triage CSV endpoints
tqdm==4.67.1
python-dotenv==1.0.1
deep-translator==1.11.4 # post-generation translation for Indian languages
nltk==3.9.4             # optional tokenization/sentence-splitting enhancements
slowapi==0.1.9         # request rate limiting

# ── Dev / Testing ──────────────────────────────────────────────────────────
pytest==9.0.2
locust==2.43.4         # load and stress testing

# ── Offline Scripts only (triage / training) ───────────────────────────────
# Install separately with:  pip install -r requirements-scripts.txt
# torch
# transformers
# scikit-learn
# joblib
# numpy
# cleanvision
