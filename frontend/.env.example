# Jan-Sunwai AI — Frontend environment variables
# Vite reads this file in ALL modes (dev + build) unless a mode-specific
# file (.env.development / .env.production) overrides it.

VITE_API_URL=http://localhost:8000/api/v1